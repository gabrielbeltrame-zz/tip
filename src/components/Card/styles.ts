import styled from "styled-components";

export const Container = styled.div`
  background: var(--shape);
  width: 400px;
  height: auto;
  margin: 0 auto;

  display: flex;
  align-items: center;
  border-radius: 10px;
`;
